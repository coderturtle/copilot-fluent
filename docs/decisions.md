# Decisions: Copilot Fluent

## Documentation convention

This log is append-only. When a later decision changes or invalidates an earlier row,
add a new dated row rather than editing the old one, and name the superseded row
explicitly (e.g. "supersedes 2026-09-12 row on X"). `heartbeat`'s own decisions.md still
describes a site `base` path that its actual `astro.config.mjs` no longer uses, because
no superseding row was ever added when the config changed — this rule exists to not
repeat that.

## ADR Log

| Date | Decision | Rationale |
|---|---|---|
| 2026-09-12 | Initial scaffold as factory-output | Teach non-technical knowledge workers to get real productivity value from enterprise Microsoft 365 Copilot (Work IQ, Copilot in Word/Excel/PowerPoint/Outlook/Teams, Researcher/Analyst agents) via hands-on exercises through a recurring fictional company scenario, gated by a required-output checklist plus an AI-graded rubric, with the module arc mapped to the AB-730 (Microsoft Certified: AI Business Professional) exam. |
| 2026-09-12 | Naming pass complete: **Copilot Fluent** | Human (coderturtle) picked "Copilot Fluent" from candidates (Copilot Fluent, Prompt Native, Second Draft, The Assist, Copilot Certified — flagged as a Microsoft-affiliation risk and not recommended, Inbox Native), all GitHub-slug-availability-checked under `coderturtle` first. GitHub repo, local dir, git remote, `.hekton/project.yaml`, and repo-local mind-palace mirror renamed from the working title `copilot-productivity-workshop` same day (PR #1, merged). Vault card at `~/vaults/hekton-mind-palace/20-projects/factory-output/` also renamed, vault mutation explicitly authorised for this specific rename per this factory's operating model. |
| 2026-09-12 | Scenario/vehicle named **Kellerman & Castle**, cast named after Patrick Swayze film characters | Per the Workshop Gremlin's named-shared-project principle (established by `borrow-native`'s `relay` and `heartbeat`'s `Checkout`): every module's exercises are drawn from one continuing fictional workplace rather than generic disconnected fixtures. Kellerman & Castle is a boutique financial-services/wealth-management advisory firm — the Swayze reference (Kellerman's resort from *Dirty Dancing*, Johnny Castle) is a deliberate in-joke that must never surface in learner-facing copy; the firm reads completely straight. Financial services was chosen deliberately (not decoratively) because it gives the AB-730 responsible-AI/data-protection domain (25-30% of the real exam) genuine stakes rather than a bolted-on compliance module. Recurring cast: Johnny Castle (manager), Baby Houseman (mentee), Sam Wheat (peer analyst), Molly Jensen (client), Wade Garrett (senior partner/mentor), Dalton (compliance), Bodhi (high-risk-client foil, reframed at Phase 1 away from investment-risk framing toward verification/data-care framing per liability guardrails), Oda Mae Brown (office manager/EA). |
| 2026-09-12 | Curriculum anchor: **AB-730, "Microsoft Certified: AI Business Professional"** | Real, current (skills version 2026-07-22), beginner-level, no-code certification — the direct non-technical parallel to `borrow-native`'s Ardan Labs certification anchor. Module arc positioned to map onto AB-730's own skills-measured outline (3 domains / 7 objective headings / 30 leaf bullets) rather than inventing a teaching sequence from scratch. coderturtle will personally sit the exam once content exists, as this workshop's dogfooding evidence — same evidentiary role Ardan played for `borrow-native`. Exam mechanics are stated only as far as Microsoft publishes them: passing score 700/1000, 45 minutes, Beginner level, Pearson VUE-proctored; question count and price are **not published** as fixed figures and must never be asserted as such (verified 2026-09-12 against Microsoft Learn directly; an earlier drafting pass asserted "$165, 40 questions" and a cross-model critique pass asserted "$99" — neither is supported by the live source). |
| 2026-09-12 | Two-tier gate design, mirroring `borrow-native`'s deterministic+conceptual split | This subject (a closed SaaS product) has no compiler/test-suite equivalent, so **Tier 1** is a required-output checklist of mechanically-checkable properties (counts, named strings, bounds, before/after diffs — never "citation-check evidence" or "curated" or "traceable", which conceal judgment rather than being checkable) and **Tier 2** is an AI-graded rubric via a supplied grading prompt, marked as an untested hypothesis (the same product both produces and grades the work — a deliberate, thematically consistent choice with a real, unaddressed circularity/injection risk, most acute in Module 02 which teaches prompt injection itself). |
| 2026-09-12 | `docs/local-assumptions.md` verify-script gap: flagged, not fixed here | `scripts/verify-project.sh --dry-run` fails on `MISSING: docs/local-assumptions.md`. Confirmed via direct check that `heartbeat`'s own `verify-project.sh --dry-run` fails identically — this is a pre-existing, cross-workshop scaffold/verify-script mismatch (the verify script checks the public repo for a file the scaffold convention deliberately keeps in the private sibling), not a copilot-fluent-specific defect. Not patched here to avoid making this repo inconsistent with its own siblings; worth a Quartergremlin/`scaffold-project.sh` fix upstream. See `docs/next-actions.md`. |
| 2026-09-12 | README's three broken links to private-sibling docs: deferred to Phase 3, not patched now | `session-log.md`, `human-understanding-check.md`, and `depth-decision.md` are linked from the raw scaffold `README.md` but live only in `copilot-fluent-private/docs/`. Confirmed `heartbeat`'s README no longer has this section at all — the established "Deliverables & branding" phase's README rework replaces this boilerplate wholesale for every workshop. Patching it now would be thrown away at that phase. |
