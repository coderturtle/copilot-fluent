# Decisions: Copilot Fluent

## Documentation convention

This log is append-only. When a later decision changes or invalidates an earlier row,
add a new dated row rather than editing the old one, and name the superseded row
explicitly (e.g. "supersedes 2026-09-12 row on X"). `heartbeat`'s own decisions.md still
describes a site `base` path that its actual `astro.config.mjs` no longer uses, because
no superseding row was ever added when the config changed — this rule exists to not
repeat that.

## ADR Log

| Date | Decision | Rationale |
|---|---|---|
| 2026-09-12 | Initial scaffold as factory-output | Teach non-technical knowledge workers to get real productivity value from enterprise Microsoft 365 Copilot (Work IQ, Copilot in Word/Excel/PowerPoint/Outlook/Teams, Researcher/Analyst agents) via hands-on exercises through a recurring fictional company scenario, gated by a required-output checklist plus an AI-graded rubric, with the module arc mapped to the AB-730 (Microsoft Certified: AI Business Professional) exam. |
| 2026-09-12 | Naming pass complete: **Copilot Fluent** | Human (coderturtle) picked "Copilot Fluent" from candidates (Copilot Fluent, Prompt Native, Second Draft, The Assist, Copilot Certified — flagged as a Microsoft-affiliation risk and not recommended, Inbox Native), all GitHub-slug-availability-checked under `coderturtle` first. GitHub repo, local dir, git remote, `.hekton/project.yaml`, and repo-local mind-palace mirror renamed from the working title `copilot-productivity-workshop` same day (PR #1, merged). Vault card at `~/vaults/hekton-mind-palace/20-projects/factory-output/` also renamed, vault mutation explicitly authorised for this specific rename per this factory's operating model. |
| 2026-09-12 | Scenario/vehicle named **Kellerman & Castle**, cast named after Patrick Swayze film characters | Per the Workshop Gremlin's named-shared-project principle (established by `borrow-native`'s `relay` and `heartbeat`'s `Checkout`): every module's exercises are drawn from one continuing fictional workplace rather than generic disconnected fixtures. Kellerman & Castle is a boutique financial-services/wealth-management advisory firm — the Swayze reference (Kellerman's resort from *Dirty Dancing*, Johnny Castle) is a deliberate in-joke that must never surface in learner-facing copy; the firm reads completely straight. Financial services was chosen deliberately (not decoratively) because it gives the AB-730 responsible-AI/data-protection domain (25-30% of the real exam) genuine stakes rather than a bolted-on compliance module. Recurring cast: Johnny Castle (manager), Baby Houseman (mentee), Sam Wheat (peer analyst), Molly Jensen (client), Wade Garrett (senior partner/mentor), Dalton (compliance), Bodhi (high-risk-client foil, reframed at Phase 1 away from investment-risk framing toward verification/data-care framing per liability guardrails), Oda Mae Brown (office manager/EA). |
| 2026-09-12 | Curriculum anchor: **AB-730, "Microsoft Certified: AI Business Professional"** | Real, current (skills version 2026-07-22), beginner-level, no-code certification — the direct non-technical parallel to `borrow-native`'s Ardan Labs certification anchor. Module arc positioned to map onto AB-730's own skills-measured outline (3 domains / 7 objective headings / 30 leaf bullets) rather than inventing a teaching sequence from scratch. coderturtle will personally sit the exam once content exists, as this workshop's dogfooding evidence — same evidentiary role Ardan played for `borrow-native`. Exam mechanics are stated only as far as Microsoft publishes them: passing score 700/1000, 45 minutes, Beginner level, Pearson VUE-proctored; question count and price are **not published** as fixed figures and must never be asserted as such (verified 2026-09-12 against Microsoft Learn directly; an earlier drafting pass asserted "$165, 40 questions" and a cross-model critique pass asserted "$99" — neither is supported by the live source). |
| 2026-09-12 | Two-tier gate design, mirroring `borrow-native`'s deterministic+conceptual split | This subject (a closed SaaS product) has no compiler/test-suite equivalent, so **Tier 1** is a required-output checklist of mechanically-checkable properties (counts, named strings, bounds, before/after diffs — never "citation-check evidence" or "curated" or "traceable", which conceal judgment rather than being checkable) and **Tier 2** is an AI-graded rubric via a supplied grading prompt, marked as an untested hypothesis (the same product both produces and grades the work — a deliberate, thematically consistent choice with a real, unaddressed circularity/injection risk, most acute in Module 02 which teaches prompt injection itself). |
| 2026-09-12 | `docs/local-assumptions.md` verify-script gap: flagged, not fixed here | `scripts/verify-project.sh --dry-run` fails on `MISSING: docs/local-assumptions.md`. Confirmed via direct check that `heartbeat`'s own `verify-project.sh --dry-run` fails identically — this is a pre-existing, cross-workshop scaffold/verify-script mismatch (the verify script checks the public repo for a file the scaffold convention deliberately keeps in the private sibling), not a copilot-fluent-specific defect. Not patched here to avoid making this repo inconsistent with its own siblings; worth a Quartergremlin/`scaffold-project.sh` fix upstream. See `docs/next-actions.md`. |
| 2026-09-12 | README's three broken links to private-sibling docs: deferred to Phase 3, not patched now | `session-log.md`, `human-understanding-check.md`, and `depth-decision.md` are linked from the raw scaffold `README.md` but live only in `copilot-fluent-private/docs/`. Confirmed `heartbeat`'s README no longer has this section at all — the established "Deliverables & branding" phase's README rework replaces this boilerplate wholesale for every workshop. Patching it now would be thrown away at that phase. |
| 2026-09-12 | **Supersedes** this row's own earlier "Work IQ" scope claim: Work IQ is out of scope as a *taught subject*, not because the term is "dated" | This row's original rationale text (and the scaffold-stage `.hekton/project.yaml` idea statement) named "Work IQ" as part of what the workshop teaches. `docs/workshop-design.md` §4 correctly scopes it out — AB-730's outline never names it — but an earlier draft of that section's reasoning called Work IQ "a dated product-marketing aside," which the Workshop Review Panel's AI/ML Practitioner persona found stale: Work IQ is a real, current, GA'd (June 2026) three-layer platform with its own docs/CLI/MCP/APIs. The scope-out decision is unchanged and correct; only the stated reason was wrong, corrected here. |
| 2026-09-12 | **Human decision:** reveal the Kellerman & Castle Easter egg to learners, once, after the capstone | The Review Panel's Developer Evangelist persona flagged permanent internal-only concealment as a wasted hook; coderturtle confirmed a post-capstone reveal (exact mechanism — footer credit, Module 09 postscript, or a dedicated page — deferred to Phase 3/4). Every module's gated exercises stay completely straight-faced; nothing here changes pedagogy, since no Tier-1/Tier-2 check keys on character identity. |
| 2026-09-12 | Workshop Review Panel (first pass) complete; 6 of 9 action-list items applied same-pass, 3 deferred | Full report: `docs/review-panel/2026-09-12-initial-design.md`. All seven personas returned distinct, non-empty findings; three genuine cross-persona convergences (honesty-notes-not-wired-to-blockers; internal vocabulary leak risk; Kellerman & Castle cast being decorative to the actual gate mechanics). Applied in this pass: Module 03's gate strengthened with real run-dependent evidence (the arc's most severe single finding — it's the hard prerequisite for Modules 04-08 and was previously presence/self-attestation-only); the Tier-1 vocabulary near-collision (`[self-attested]` vs. "self-administered"/"mechanically graded") resolved with an explicit glossary table; four Skeptical-Critic overclaim instances fixed (§7's "not left uncovered by either tier," "flagship," the unearned `borrow-native` comparative, §6's unhedged success-criterion framing); §9's "one continuing story" softened to reflect that the capstone deliberately introduces disconnected new facts per module (anti-gaming, by design); Module 02's injection-mitigation given a stated content-time validation step and a missed-teaching-moment note; the sensitive-data Tier-1 item bound explicitly to §12's actual teaching point. Deferred, tracked in `docs/next-actions.md`, not silently dropped: revealing the Kellerman & Castle Easter egg post-capstone (a brand/tone call for Phase 3, not a doc-text fix); §7's table-as-embedded-essays structural density (a Phase-3 module-README-authoring concern); the site/fixture-upload mechanics gap the End-User persona flagged (already Phase 4's scope per §13). |
| 2026-09-12 | Site built at `base: "/"` on a custom domain (`copilot-fluent.coderturtle.io`), not GitHub Pages' `/copilot-fluent/` project-path default | Checked all four prior workshop sites directly rather than assuming: every one (`terminal-velocity`, `borrow-native`, `half-life`, `heartbeat`) now runs `base: "/"` on its own `<slug>.coderturtle.io` domain via agentic-infra-lab's `github-pages-dns` pattern. `heartbeat`'s own decisions.md still described the stale `/heartbeat/` project-path default at the time this workshop's design doc was drafted, because no superseding row was ever added when that config changed - the exact failure mode this repo's own append-only-with-superseding-rows convention (top of this file) exists to prevent. DNS is not yet live for `copilot-fluent.coderturtle.io` as of this commit; do not run `workflow_dispatch` until `docs/next-actions.md`'s DNS/verification/Pages-enablement steps are confirmed. |
| 2026-09-12 | Second Astro content collection added for `modules/*/README.md`, extending the reference pattern | All four prior workshop sites exposed only a `buildlog` collection (maintainer journal). This is the first workshop where learners read modules on the published site itself (a no-git audience) - see `docs/workshop-design.md` §3. Route-ID bug found and fixed before it shipped: Astro 5's glob-loader default ID generator strips only a trailing `/index`, not `/README`, which would have produced URLs like `.../readme` instead of clean module slugs. Fixed with an explicit `generateId` in `site/src/content/config.ts`; verified in the built `dist/` output. Module titles are extracted from each README's own first heading via `render()`'s returned `headings`, not frontmatter, since adding YAML frontmatter to files meant to be read first on GitHub would make them uglier there. |
| 2026-09-12 | `npm audit` run fresh, not inherited from `heartbeat`'s recorded disposition | 5 vulnerabilities (3 low, 1 high, 1 critical), all from the shared `astro ^5.0.0`/`@astrojs/mdx`/`@astrojs/tailwind` pin every workshop site carries, requiring a breaking major `astro` bump to clear. Not the same as `heartbeat`'s own recorded "3 low, 2 high" from 2026-08-29 - the count held but severity worsened into a critical since then, confirming this drifts rather than staying static. Not fixed here (cross-workshop dependency decision, `dependency_changes: human_required`); recorded as RISK-0002 in `docs/risks.md`. |
| 2026-09-12 | Known limitation shipped, not fixed: module READMEs' relative links 404 on the published site | `../02-trust-but-verify/README.md`-style links render fine on GitHub but resolve to a non-existent route on the site (confirmed by inspecting built `dist/` HTML - no fixtures/content exist yet to make this a live problem today). Needs a real fix (a remark link-rewrite plugin, or an authoring convention using absolute GitHub URLs) before real module content ships. Recorded as RISK-0003 and in `docs/next-actions.md`, not silently left for a future session to rediscover. |
| 2026-09-12 | **Standing rule adopted mid-session:** every phase gets a cross-model (Codex) review before being called done, not just the design doc | Phase 1 got 3 DDD cycles; Phases 3-4 initially got only mechanical checks (build/lint/link-check). coderturtle asked directly whether everything was being pair-reviewed with a different model - it wasn't, for Phases 3-4. Retroactive review found 22 issues cycle 1, 12 more cycle 2, 6 more cycle 3 (converging, not diverging) - see RISK-0004 for the full list and severity. Most severe: the site was about to publish module skeletons' internal build vocabulary directly as live learner content, a real `docs/brand.md` violation neither a single-perspective build nor mechanical checks caught. All fixed; verified by rebuilding the site and re-running the lint against planted violations (including two the lint itself had missed) after each fix round. Stopped at the process's 3-cycle ceiling per `doubt-driven-development`'s own rule, with cycle 3's findings small enough (a misleading "cutover landed" comment, a lint false-positive class the fix itself introduced, a latent cross-workshop `canonicalUrl` bug inherited from `heartbeat`'s own layout) to treat as converged rather than escalate further. |
| 2026-09-12 | Phase 5 (non-blocking): "Fifth Run" section, plus a new "Variant: Non-Engineering Audience Workshops" section, written back into `~/hekton/gremlins/workshop/workshop-gremlin.md` | On branch `agent/claude/copilot-fluent-fifth-run-writeback` in `~/hekton` (a separate repo from this one). Also given the same cross-model review discipline as everything else in this build, since it's shared canonical infrastructure future workshops will read: a first pass found the write-back had repeated the exact 26/5/0 coverage-count error already caught and fixed once in this repo's own `docs/workshop-design.md`, misattributed which review-chain stage proposed which wrong exam price, overclaimed a specific GitHub Pages CNAME mechanism after that exact overclaim had already been walked back once in `.github/workflows/deploy-pages.yml`, and cited unverifiable scenario-naming candidates not actually present in this repo's own decisions log — all real, all ironic given the write-back's own subject matter, all fixed. New Design Principle 5 added to the Gremlin's own contract, formally encoding the cross-model-review standing rule rather than leaving it as prose only. |
