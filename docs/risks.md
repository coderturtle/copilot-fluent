# Risks: Copilot Fluent

## Risk Register

Machine-readable risk state lives in `.hekton/risk-register.yaml`. Keep this
Markdown file as the human-readable explanation of material risks and mitigations.

| ID | Date | Risk | Impact | Likelihood | Mitigation | Status |
|---|---|---|---|---|---|---|
| RISK-0001 | 2026-09-12 | Initial governance baseline needs first human/agent review | Medium | Medium | Run governance preflight and end-session review during the first material session | Open |
| RISK-0002 | 2026-09-12 | `site/`'s npm dependencies (`astro ^5.0.0`, `@astrojs/mdx`, `@astrojs/tailwind`) carry 5 known vulnerabilities (3 low, 1 high, 1 critical) as of a fresh `npm audit` today, all requiring a breaking major `astro` bump to clear. Same shared pin every prior workshop site (`terminal-velocity`, `borrow-native`, `half-life`, `heartbeat`) also carries - `heartbeat`'s own count has already worsened from "3 low, 2 high" (2026-08-29) to this same 3 low/1 high/1 critical split, confirming this drifts, not just accumulates. Not fixed here; a cross-workshop dependency decision, not a copilot-fluent-only one. | Medium (build-time only, static site, no runtime user input) | Confirmed present | Open, cross-workshop |
| RISK-0003 | 2026-09-12 | Module READMEs' relative links (e.g. `../02-trust-but-verify/README.md`) and module-to-fixture links (e.g. `fixtures/firm-overview.md`) render correctly on GitHub but would 404 on the published site. Live once Modules 01-03 shipped real fixtures - confirmed by a cross-model review, not just a theoretical future case. | Was: low today, rising with content; realized once Modules 01-03 shipped fixtures | Certain once content exists | **Fixed 2026-09-13**, verified by rebuild, `astro check`, built-`dist/` HTML/file-path inspection, and local `curl` against several rewritten links - see `docs/decisions.md`, 2026-09-13 row |
| RISK-0004 | 2026-09-12 | A retroactive cross-model (Codex) review of Phase 3+4 - the standing rule going forward is every phase gets this before being called done, not just the design doc - found 22 real issues a single-perspective build and mechanical checks (build/lint/link-check) had missed. Most severe: the site was about to publish module skeletons' internal build vocabulary ("Tier 1," "Coachgremlin," "self-attested") directly as live learner-facing content, a real `docs/brand.md` violation; a real title-extraction bug on the live modules index; forbidden AB-730 price/question-count figures shipped verbatim in the build log; several exam-positioning overclaims; factually wrong GitHub Pages custom-domain documentation in the deploy workflow; and real drift between several module gates and the hardened design doc. All 22 fixed, verified by rebuilding the site and re-running the lint against planted violations. Two further cross-model cycles followed the same discipline as the design doc's own DDD process: cycle 2 found 12 more real issues (a UX regression from the cycle-1 fix itself - skeleton pages lost their visible heading - plus remaining gate-description drift, a GitHub Pages custom-domain claim that was still wrong in its corrected form, and Work-IQ scope-out gaps missed in `AGENTS.md`/`project-walkthrough.md`); cycle 3 found 6 smaller issues (a misleading "cutover landed" comment, a lint-fix-introduced false-positive class, a latent `canonicalUrl` bug inherited unfixed from `heartbeat`'s own layout). Findings shrank each cycle rather than growing, and stopped at the process's 3-cycle ceiling per `doubt-driven-development`'s own rule. See `docs/decisions.md` for the full trail. | High (several were live-shippable defects) | Confirmed present, now fixed | Fixed, verified across 3 cycles |
| RISK-0005 | 2026-09-13 | Module 07's own doubt-driven-development pass found, and a live `npm run build` confirmed, that the `fixtures` Astro content collection added to close RISK-0003 (`site/src/content/config.ts`, `pattern: "*/fixtures/*.md"`) globs and statically renders **every** file directly under a module's `fixtures/` directory with no exclusion for grading-only material - `dist/modules/01-starting-at-kellerman-and-castle/fixtures/grading-key/index.html` and `dist/modules/02-trust-but-verify/fixtures/grading-key/index.html` both build and both contain their modules' real hidden facts in the clear. This defeats Lesson 3's reveal-after-commitment design for both of those modules: no honor system is needed, the answer is a guessable URL away. Module 07's own hidden key avoids this by living one directory level deeper (`fixtures/hidden/action-items-key.md`), a path the current glob genuinely does not match (verified by rebuild - confirmed no `dist/.../fixtures/hidden/...` output is produced). Modules 01 and 02's existing `grading-key.md` files are **not fixed by this row** - moving another module's already-shipped fixture was judged out of this pass's scope and a collision risk against other in-progress work on this branch. | High (defeats the honor-system gate design for 2 shipped modules; the answer is live on the published site today) | Confirmed present via actual build output, not theoretical | Open for Modules 01-02; closed for Module 07 by file placement (see `docs/decisions.md`, 2026-09-13 Module 07 row) |
